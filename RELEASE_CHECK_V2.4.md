# V2.4 发布检查

## 本次范围

- 桌面静态图与视频的 L/M/H 改变内部采样尺度，桌面输出最长边保持 1920。
- NICE TRY 使用选定档位计算，不再安排延迟二次细化。
- 不发布 Light Dots；移除 Focus Melt 的控制界面、交互层和渲染实现。原 Thermal Map 与 Graphic Decor 的既有效果保留。
- 保留隐藏 History 和 Undo；不发布独立实验页面或新素材目录。

## 本轮修复

- 质感切换后立即同步配方码；Undo 恢复档位。
- 取消旧颜色更新、图片细化和视频细化任务，防止覆盖新的 NICE TRY。
- PNG 导出先计算当前结果，并在异步编码前固定配方与预设名称。
- 未上传素材、输出期间不能执行 Undo；避免重复图片输出。
- 移动端不显示新档位入口，保留 GT2 配方和原有移动端采样分支。

## 已验证

- `node tools/check-release.cjs`：脚本语法、实验功能排除、四预设×三档的配方往返、GT2/GT3 解码、移动端 GT2、横竖图输出比例、视频编码偶数尺寸、静态资源存在。
- 桌面浏览器：SVG 上传；L/M/H 分别产生最长边 720/1080/1920 的计算画布；切换更新配方；Undo 从 M 恢复 L；NICE TRY 在 H 结束时仍为 H；复制状态反馈。
- 页面导出流程达到“图片已输出：L 质感 · 1920px”，无控制台错误。自动化未捕获下载事件，因此不能视为下载文件落盘及重新导入的完整验证。

## 已知限制及后续建议

- H 档 CPU 运算可阻塞主线程数秒。后续可评估 Worker/分块计算；本次不改动原有效果算法。
- 参数拖动仍使用低档预览后细化，和 NICE TRY 的单次最终计算不同；需另行决定是否统一交互。
- 视频编码改动进行了代码审阅和尺寸单测，本轮未完成真实视频的完整编码、暂停/继续、重新导入测试；Safari/iOS 实机也未覆盖。
- 浏览器下载、剪贴板及外部 PDF/编码库存在环境差异，不能以本轮测试保证所有浏览器无问题。
- 导入处理过的 PNG 可以保留既有画面和配方，但若要改变其质感，并不能从成品反推出未处理的原始照片。完整无损再编辑仍需要原图。

本检查聚焦发布链路，不等同于全量安全审计或所有设备的兼容性认证。
